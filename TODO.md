# udemy-clone TODO

- [ ] Create full `server/` directory scaffold (config/models/controllers/routes/middleware/services/utils/validations/jobs)
- [ ] Add baseline server entry wiring (Mongo connect, route mounting, error handler)
- [ ] Update `server/server.js` to mount `/api/*` routes and keep `/health`
- [ ] Ensure `server/package.json` has required dependencies for scaffold
- [ ] Run `npm install` in `server/`
- [ ] Run `npm run dev` in `server/` and verify `/health` + sample `/api/health` route

